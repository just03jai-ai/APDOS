{
  "name": "@apdos/delivery-workflow",
  "version": "0.1.0",
  "description": "Delivery Workflow V1 for APDOS.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm --workspace @apdos/discovery-agent run build && npm --workspace @apdos/product-agent run build && npm --workspace @apdos/design-agent run build && npm --workspace @apdos/architecture-agent run build && npm --workspace @apdos/engineering-agent run build && npm --workspace @apdos/qa-agent run build && npm --workspace @apdos/governance-agent run build && tsc -p tsconfig.json && node --test dist/tests/**/*.test.js"
  },
  "dependencies": {
    "@apdos/architecture-agent": "0.1.0",
    "@apdos/approval-engine": "0.1.0",
    "@apdos/artifacts": "0.1.0",
    "@apdos/context-engine": "0.1.0",
    "@apdos/discovery-agent": "0.1.0",
    "@apdos/design-agent": "0.1.0",
    "@apdos/engineering-agent": "0.1.0",
    "@apdos/governance-agent": "0.1.0",
    "@apdos/product-agent": "0.1.0",
    "@apdos/qa-agent": "0.1.0",
    "@apdos/skill-runtime": "0.1.0",
    "@apdos/validation-engine": "0.1.0",
    "@apdos/workflow-engine": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
